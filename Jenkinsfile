pipeline {

    stages {

        stage('setup') {
            echo "setup"
        }

        stage('load databases') {
            echo "load df's"
        }

        stage('compile') {
            echo "compile"
        }

        stage('package') {
            echo "package"
        }

        stage('test') {
            echo "test"
        }

    }
}